# แผนปรับปรุงปลั๊กอิน → 1.10.0

**ที่มา:** `timelapse_master_prompt.md` — วัดรีล 6 ตัวของเพจ **แบบบ้านโมเดิร์น** (9–50 ล้านวิว)
เมื่อ 15 ส.ค. 2569 ด้วย `tools/study-clip.mjs`

**ตัวเลขที่ขับแผนนี้:** เขา **9–28 ช็อตต่อคลิป ช็อตละ 0.63–1.29 วิ** · เรา **3 ช็อต ช็อตละ 8 วิ**
ความยาวคลิปพอกัน (15–30 เทียบ 26 วิ) **ปัญหาอยู่ที่จำนวนเหตุการณ์ทางภาพ ไม่ใช่ความยาว**

---

## สิ่งที่ตั้งใจ **ไม่** แตะ — ตัดสินไปแล้ว

| ไม่แตะ | เพราะ |
|---|---|
| **กล้องนิ่งท้ายคลิป** | ผู้ใช้เลือกแล้ว 15 ส.ค. 2569 · ท้ายคลิปเป็นได้ทั้งนิ่ง/เข้า/ออก ไม่มีอันไหนผิด แต่กล้องนิ่งทำให้เฟรมจบยังตรง `phase4.png` เป๊ะโดยไม่ต้องเสี่ยง |
| **ภาพ 4 เฟสไม่มีคน** | รอยต่อ F2F ต้องสะอาด และคลิปดัง 6/6 ก็จบด้วยเฟรมไม่มีคนเหมือนกัน |
| **`no camera movement whatsoever` ในตัวช็อต** | วัดแล้ว 6/6 คลิป กล้องนิ่งสนิทในทุกช็อต — กฎนี้ถูกอยู่แล้ว |
| **โครง 8 บล็อก ไม่แปลงเป็น JSON** | ยังไม่เคยทดสอบ JSON กับโมเดลวิดีโอ · วิดีโอแพงที่สุด |
| **P3 ต้องสะอาด ไม่มีวัสดุ** | เป็นเฟรมโชว์สินค้าครบชุด |
| **จบที่พลบค่ำ** | เจอแค่ 2/6 · ทำให้ `check-scene` ด่าน `frames` แดงแน่นอน **ยกไว้ก่อน** |

---

# ส่วนที่ 1 — ฝั่งภาพ 4 เฟส

> **ตัดสินแล้ว 15 ส.ค. 2569 — ฝั่งภาพแตะแค่ 2 ข้อ**
> ข้อเสนอเดิมที่จะเพิ่มช่อง `โซนโคลสอัพ` ใน CAMERA LOCK CARD · บล็อก "ระยะใกล้เห็นอะไร"
> ใน `design.md` · และกลุ่ม `closeup` ใน `phase4.json` — **ยกเลิกทั้งสามข้อ**
> เรื่องโคลสอัพจัดการที่พรอมต์วิดีโออย่างเดียว (ดูหัวข้อ 2.7) ไม่ต้องมีโครงสร้างข้อมูลรองรับ

## 1.1 ⭐ `idea` — P4 ต้องหยุดซีด (ข้อที่ผู้ใช้สังเกตเอง · วัดยืนยันแล้ว)

วัดด้วย `ffmpeg -vf signalstats`: **ความอิ่มสีของภาพจบเขา ค่ากลาง 22.5 · ของเรา 12**
และภาพเรา**สว่างกว่า** (YAVG 120 เทียบ 97) — สว่างกว่าแต่สีจางกว่า = ภาพซีด

**ไม่ต้องเปลี่ยนเวลาเป็นพลบค่ำ** 33m กับ 29m เป็นภาพกลางวันเห็นทุ่งเขียวสว่างนอกหน้าต่าง
แต่เปิดไฟในบ้านไว้ ได้ 22.5 กับ 21.9 ชนะภาพที่ดีที่สุดของเรา (18.9)
→ **ไม่ชนกับด่าน `frames` ที่ตรวจความสว่างไม่กระโดด**

### แก้ 4 จุดในพรอมต์ P4

**ก. คีย์ `lighting` — ต้องระบุว่ามีไฟดวงจริงติดอยู่ในเฟรม**

ภาพเราไม่มีไฟติดเลยสักดวงในทั้ง 6 ฉาก ของเขาติดทุกใบ
เพิ่มเป็นข้อบังคับ: ระบุ **โคมไฟอย่างน้อย 2 ดวงที่ติดอยู่และเห็นตัวโคมในเฟรม**
พร้อมบอกว่าแสงอุ่น (~2700K) และ**เห็นวงแสงตกลงบนพื้นผิว** ไม่ใช่แค่ "มีไฟ"

**ข. คีย์ `scene` — ต้องมีบัญชีของแต่งขั้นต่ำ**

ของเรา ~5–6 ชิ้น พื้นโล่งกินพื้นที่เยอะ · ของเขา ~10–12 ชิ้นเป็นชั้นๆ
เขียนเป็นรายการบังคับในสกิล: **พื้น** (พรม) · **ที่นั่ง** (หมอน 2–3 ใบ) ·
**พื้นผิว** (ถาด แก้ว หนังสือ) · **ต้นไม้ 3 กระถางคนละความสูง** · **ชั้นผนังมีของ 3–5 ชิ้น**

**ค. คีย์ `palette` — ต้องมีผ้าที่มีลายและมีสีอย่างน้อย 1 ชิ้น**

ภาพเรานิวทรัลล้วนทั้ง 6 ฉาก (เบจ เทา ไม้ ขาว) ของเขามีผ้าคลุมลายตาราง หมอนลายดอก พรมทอลาย
**สีตัดหนึ่งจุดคือสิ่งที่ทำให้สายตาหยุด**

**ง. คีย์ `camera` — เพิ่มของบังหน้าเฟรมสร้างระยะ**

⚠️ ของชิ้นนี้ต้องเป็น**สถาปัตยกรรม ไม่ใช่ของแต่ง** (ขอบประตู ขอบเสา ขอบชั้นที่ติดผนัง)
เพราะมันต้องอยู่ครบทั้ง 4 เฟส ถือเป็นกลุ่ม `fixed` ใน `phase4.json`
ถ้าใส่เป็นของแต่งจะกลายเป็น styling-leak ทันที

### จุดตรวจใหม่ — `check-scene.mjs` ด่าน `frames`

| ตรวจ | เกณฑ์ | ระดับ |
|---|---|---|
| `SATAVG` ของ `phase4.png` | **≥ 18** | **WARN** |

เกณฑ์ 18 มาจาก: ของเขา 4/5 ใบผ่าน · ของเรา 1/6 ใบผ่าน — เป็นเส้นที่แยกสองกลุ่มได้จริง

⚠️ **SATAVG เป็นตัวชี้ ไม่ใช่ความสวย** ภาพอิ่มสีจัดแต่รกก็ได้คะแนนสูง
ใช้เป็นด่านกันภาพซีดเท่านั้น เป็น WARN ไม่ใช่ FAIL และต้องดูภาพจริงเสมอ

## 1.2 `phase2` — P2 ต้องเป็น "งานที่ค้างอยู่กลางจังหวะ" ไม่ใช่ "วัสดุกองรอ"

**นี่คือข้อที่เปลี่ยนเนื้อภาพจริง** คลิปดังไม่มีใบไหนที่สภาพกลางเรื่องเป็นกองของรอ
มันเป็นงานที่ทำค้างไว้ — พื้นปูไปครึ่งห้องแล้วค้อนยางวางอยู่ · ผนังติดไปแล้วสามแผ่น
(หลักฐาน: `17m-grid.png` ที่ 10.0–14.0 วิ · `29m-grid.png` ช่วงปูพื้น)

เปลี่ยนคำสั่งใน `phase2/SKILL.md` จาก "วัสดุกองรออยู่ข้างๆ" เป็น:

> **P2 ต้องแสดงงานที่เดินไปได้ครึ่งทางแล้ว** — ส่วนที่เสร็จกับส่วนที่ยังไม่เสร็จอยู่ในเฟรมเดียวกัน
> พร้อมเครื่องมือที่ใช้ทำงานนั้นวางอยู่ตรงจุดที่ทำค้าง
> ห้ามเป็นกล่องปิดผนึกกองรวมกัน — กล่องปิดไม่มีจังหวะให้ถ่ายใกล้
> **วัสดุที่อยู่ในเฟรมยังต้องเป็นวัสดุของงาน P2→P3 เท่านั้น** (กฎเดิม ไม่เปลี่ยน)

ผลพลอยได้: `seg2` ได้ทั้งจุดโคลสอัพและเสียง ASMR ที่มีที่มาในเฟรมจริง

---

# ส่วนที่ 2 — `Action:` ในพรอมต์วิดีโอ

## 2.1 ปัญหาที่วัดได้

`seg2` ปัจจุบันมี **7 รอบ `walk in … walk out` ใน 8 วินาที** = รอบละ ~1.1 วิ
ซึ่งต้องแบ่งเวลาให้ **เดินเข้า + ทำงาน + เดินออก** ครบในนั้น
คลิปดังใช้เวลาต่อขั้นพอๆ กัน (0.6–1.3 วิ) แต่ **ทั้งช็อตเป็นการทำ** ไม่มีเดินเลย

## 2.2 กฎใหม่ 4 ข้อ (แทนของเดิม)

| ข้อ | เดิม | ใหม่ |
|---|---|---|
| 1 | ทุกวรรคขึ้นต้นด้วย `a worker` / `two workers` | **ไม่เปลี่ยน** |
| 2 | วรรคที่มีคนต้องปิดด้วย `and walks out` | **เฉพาะวรรคสุดท้าย** |
| 3 | คั่นด้วย semicolon เรียงตามเวลา | **ไม่เปลี่ยน** + **เหลือ 3–4 วรรค ไม่ใช่ 7–8** |
| 4 | ปิดด้วย `No instant appearances.` + `The final second is completely still and empty of people.` | **ไม่เปลี่ยน** |

**เพิ่มข้อ 5:** ทุกวรรคต้องมี**กริยาที่ทำซ้ำเป็นจังหวะได้** — `presses` `taps` `slots`
`tightens` `smooths` `peels` ไม่ใช่ `lifts` `places` เฉยๆ
เพราะจังหวะคือสิ่งที่ทำให้ช็อตมีอะไรให้ดูตลอด 2 วินาที และเป็นที่ที่โคลสอัพไปเกาะ
(คลิปดังคัตรัวตรงงานที่มีจังหวะเสมอ — `29m` 5 ช็อตใน 1.7 วิ ตอนเคาะค้อนปูพื้น)

## 2.3 ⭐ ลำดับการยกของเข้ามา — กฎใหม่ 3 ข้อ

**นี่คือสิ่งที่ทำให้คลิปเขาดูเป็นงานจริง และเป็นข้อที่ `Action:` ของเราไม่มีเลย**

### ข้อ 6 — เรียงตามตรรกะงานจริง 3 ช่วง ตรงกับ 3 คลิป

```
A เตรียมพื้นที่   A1 ทำความสะอาด/ปรับพื้นที่ → A2 ลงฐาน/พื้น (เริ่ม)
B งานติดตั้ง      B1 ลงฐาน/พื้น (จบ) → B2 งานที่ยึดติดกับที่ + ไฟที่ติดผนังสว่างขึ้น
C ยกของเข้ามาจัด  C1 ของหนัก ยก/ลาก → C2 ของเล็กทีละ 1-2 ชิ้น → C3 ชิ้นเอกที่กางออก + โคมไฟตั้งพื้น
```

**เลขวิ่งไปข้างหน้าอย่างเดียว ไม่ย้อนกลับ** และรอยต่อของช่วงตรงกับรอยต่อของเฟสพอดี

⚠️ **"ไฟ" เป็นสองอย่างคนละขั้น อย่ารวมเป็นข้อเดียว** —
**ไฟที่ยึดติดอาคาร** (โคมผนัง โคมเพดาน ไฟใต้ชั้น) อยู่ **B2** สว่างขึ้นตอนงานติดตั้งแล้วค้างยาวจนจบ ·
**โคมไฟตั้งพื้น/ปักดิน** ที่ยกเข้ามาวางได้ อยู่ **C3** เป็นของแต่งชิ้นสุดท้าย

**อ่าน `-grid.png` ครบทั้ง 6 ใบทีละเฟรมแล้ว ลำดับตรงกันทุกใบ** ทั้งในบ้าน (29m · 17m · 33m · 9m)
และนอกบ้าน (50m · 25m)
**ไม่มีคลิปไหนเอาของแต่งเข้ามาก่อนพื้นเสร็จ** และ**ไฟเป็นของชิ้นสุดท้ายที่เข้ามาเสมอ**

⚠️ **แต่ไฟ "ติด" ตั้งแต่กลางคลิป ไม่ใช่ติดวินาทีสุดท้าย** — `25m` ไฟผนังติดที่ 10.0 วิ
(เหลืออีก 19 วิ) · `9m` ไฟใต้ชั้นติดที่ 8.5 วิ แล้วค้างยาวจนจบ
ของที่เข้ามาทีหลังจึงถูกส่องด้วยไฟอุ่นทั้งหมด

**แปลงเป็นคำสั่งของเรา:** ไฟต้องอยู่ใน `phase3.png` **ติดอยู่แล้ว** ไม่ใช่มาโผล่ใน P4
ไม่งั้น seg3 ต้องเสียหนึ่งเหตุการณ์ไปกับการเปิดไฟ และของที่จัดใน seg3 จะไม่ได้แสงอุ่นนั้น
→ **ปรับหัวข้อ 1.4 (ก): โคมไฟที่ติดอยู่เป็นกลุ่ม `fixed` ไม่ใช่ `styling`** ต้องมีตั้งแต่ P3

### ⭐ ช่วง A/B/C ลงคลิปไหน — โมเดลสร้างได้ไม่เกิน 10 วินาทีต่อคลิป

**ข้อจำกัดที่ตายตัว: 1 คลิป ≤ 10 วินาที** ยัดทุกขั้นลงคลิปเดียวไม่ได้
สามช่วงจึงตรงกับสามคลิปแบบหนึ่งต่อหนึ่ง

| คลิป | เฟส | ช่วง | ขั้น | ~เหตุการณ์ |
|---|---|---|---|---|
| **seg1** 8 วิ | P1 → P2 | **A** เตรียมพื้นที่ | A1 ทำความสะอาด/ปรับพื้นที่ → A2 ลงฐาน/พื้น (เริ่ม) | 5–7 |
| **seg2** 8 วิ | P2 → P3 | **B** งานติดตั้ง | B1 ลงฐาน/พื้น (จบ) → B2 งานที่ยึดติดกับที่ + ไฟผนังสว่างขึ้น | 5–7 |
| **seg3** 8 วิ | P3 → P4 | **C** ยกของเข้ามาจัด | C1 ของหนัก → C2 ของเล็กทีละ 1–2 ชิ้น → C3 ชิ้นเอกที่กางออก + โคมไฟตั้งพื้น | 6–8 |

**ทำไมแบ่งแบบนี้:**

- **ขั้นลงฐาน/พื้นคร่อมสองคลิป (A2 → B1) ตั้งใจ** — เป็นขั้นที่กินเวลานานที่สุดทุกใบ
  (`29m` ปูพื้น 6–14.5 วิ = 8.5 วิ · `50m` งานฐาน 2.5–13.5 วิ = 11 วิ) **เกินเพดาน 10 วิของโมเดล**
  และ **P2 = "ระหว่างทำ" ก็คือกลางขั้นนี้พอดี** รอยต่อจึงลงตัวโดยไม่ต้องฝืน
- **ไฟผนังสว่างขึ้นใน B2 ท้าย seg2** ตามที่วัดมา (`25m` 10.0 วิ · `9m` 8.5 วิ)
  แล้วของที่จัดใน seg3 ได้แสงอุ่นนั้นทั้งหมด
  → **`phase3.png` ต้องมีไฟติดอยู่แล้ว** โคมที่ยึดติดอาคารเป็นกลุ่ม `fixed` ไม่ใช่ `styling`
- **C จบด้วยชิ้นเอกที่กางออก** ซึ่งเป็นเหตุการณ์ที่ยาวที่สุดของคลิป และเป็น payoff
  (`50m` กางร่ม 20–24 วิ · `25m` กางร่ม 21.5–24.5 วิ) แล้วปิดด้วยโคมไฟตั้งพื้น

⛔ **ขั้นที่อยู่คนละช่วงห้ามสลับที่กัน** — ของแต่ง (C) โผล่ใน seg2 = styling-leak ·
งานติดตั้ง (B) โผล่ใน seg3 = ได้ลำดับเข้า-ออก-เข้า ซึ่งเป็นบั๊กที่เคยเจอมาแล้วสองฉาก

### ข้อ 7 — กริยาต้องตรงกับน้ำหนักของและจำนวนคน

| น้ำหนัก/ขนาด | ต้องเขียนกริยาว่า | ห้ามเขียนว่า | หลักฐาน |
|---|---|---|---|
| **หนักและใหญ่** เตียง โซฟา ตู้ | `drags` `slides` `pushes` — ถัดไปกับพื้น | `carries` `lifts` | `29m` 17.5 วิ ก้มลากโครงเตียงเข้ามา |
| **ยาวเป็นมัด** ไม้ปูพื้น แผ่นระแนง | `carries in one bundle` แล้ว `unpacks` | เดินเข้าออกหลายเที่ยว | `29m` 5.0 วิ หอบมัดเดียวจบ |
| **เล็ก** จาน หมอน ของบนชั้น | `places one at a time` | วางรวดเดียวทั้งกอง | `33m` 4.5–7.5 วิ ชิ้นละช็อต |

**นี่คือเหตุผลที่คลิปเขาสมจริง — เขามีคนเดียว ฟิสิกส์เลยบังคับวิธียกของ**
พรอมต์เราเขียน `two workers` ซึ่งยกอะไรก็ได้ ของหนักเลยไม่เคยถูกลาก
→ **พิจารณาลดเหลือ `a worker` คนเดียวเป็นค่าเริ่มต้น** แล้วใช้ `two workers` เฉพาะของที่คนเดียวยกไม่ไหวจริง

### ข้อ 8 — ของกลุ่มเดียวกันต้องสะสมให้เห็นเป็นขั้น

`29m` ราวแขวนเสื้อ: **26.5 วิ = 3 ตัว → 27.0 วิ = 6 ตัว → 27.5 วิ = เต็มราว**

กฎ `No instant appearances` ที่เรามีห้ามแค่ "ของโผล่มาเอง"
ข้อใหม่นี้ไปไกลกว่า: **ของกลุ่มเดียวกันต้องเพิ่มขึ้นทีละนิดให้เห็น ไม่ใช่มาครบในเทคเดียว**
เขียนในวรรคว่า `hangs the first few shirts, then fills the rail` แทน `hangs the shirts`

### ⚠️ ความขัดแย้งกับหัวข้อ 1.4 — ต้องนับเป็นเหตุการณ์ ไม่ใช่นับเป็นชิ้น

1.4 (ข) บอกว่า P4 ต้องมีของ ~10–12 ชิ้น แต่ seg3 มี 8 วิ **ยัดการวาง 12 ครั้งไม่ได้**

`29m` ใช้ 9.5 วิ กับ **~9 เหตุการณ์ ≈ 1 วิ/เหตุการณ์** ซึ่งทำได้จริง
ทางออก: **หนึ่งวรรคใส่ได้ 2 เหตุการณ์** (`drags the bed frame in and lowers the mattress onto it`)
และของบางกลุ่มยกเข้ามาเป็นชุด (ถาดที่มีของ 3 ชิ้น)

→ **เป้าของ seg3: 3–4 วรรค · 6–8 เหตุการณ์ · ของใน P4 ที่เหลือต้องอยู่ใน P3 อยู่แล้ว**
ของที่ "อยู่ใน P3 อยู่แล้ว" ต้องไม่ใช่กลุ่ม `styling` ใน `phase4.json` ไม่งั้นเป็น styling-leak

### 🔑 ทางออกที่ดีกว่า — ให้ของชิ้นเดียวกินเวลาหลายวินาที

`25m` 20.5→24.5 วิ ใช้ **4 วินาทีเต็มกับร่มคันเดียว**: พับอยู่ → เอียงขึ้น → เริ่มกาง → กางเต็ม
เป็นช่วงที่ยาวที่สุดของครึ่งหลังคลิป และภาพเปลี่ยนตลอดโดยไม่ต้องมีของเพิ่มสักชิ้น

**ไม่ต้องมีของ 12 ชิ้นถึงจะเต็มเฟรม** ของชิ้นใหญ่ที่ **กางออก / คลี่ออก / ยกขึ้น / พับลง** ได้
ทำหน้าที่นี้ได้ดีกว่าของเล็ก 5 ชิ้น

⭐ **สินค้าของเราเข้าข่ายนี้ตรงๆ** — ศาลา ร่ม กันสาด ฉากกั้นพับได้ ชิงช้า
ให้ `idea` เลือกไอเดียที่**สินค้าเองมีจังหวะกางออก** แล้วยกจังหวะนั้นเป็นเหตุการณ์ที่ยาวที่สุดของ seg3

### ⛔ เพดาน — ของแบบนี้ได้ไม่เกิน 2 ชิ้นต่อคลิป

**ของที่กินเวลา ≥ 2 วินาที ใส่ได้มากสุด 2 ชิ้นในคลิปเดียว** เกินกว่านี้คลิปน่าเบื่อทันที
เพราะภาพค้างอยู่กับของชิ้นเดิมนานเกินไป คนดูเลื่อนผ่าน

คำนวณจากงบ 8 วินาที: **2 ชิ้น × 2–3 วิ = 4–6 วิ** เหลือ **2–4 วิ** ให้เหตุการณ์สั้น 2–4 อย่าง
รวมแล้วยังอยู่ในเป้า 6–8 เหตุการณ์ต่อคลิปพอดี

ตรงกับที่วัดมา — `25m` ครึ่งหลังมีของยาวแค่ **ร่ม 4 วิ** ชิ้นเดียว
ที่เหลือเป็นของสั้นทั้งหมด (เก้าอี้ไข่ 1.5 วิ · โต๊ะ 0.5 วิ · เก้าอี้ · โคมไฟ)

## 2.4 ⚠️ ข้อที่ต้องเขียนกำกับไว้ให้ชัด กันเข้าใจผิด

**การตัด `walks in` ออก ไม่ได้ทำให้กลับไปเป็น passive voice**

บั๊กแขนลอยเกิดจากการเอา**สิ่งของ**ขึ้นเป็นประธาน (`a cushion is carried in and placed`)
ไม่ได้เกิดจากการไม่มีคำว่า `walks in` ประโยคที่ขึ้นต้นด้วยคนแล้วลงมือทำเลย
(`a worker presses each deck tile flat`) **ยังเป็น active voice เต็มตัว คนยังเป็นประธาน**

กฎข้อ 1 จึงยังเป็นกฎที่แข็งที่สุดของบล็อกนี้ **ห้ามรื้อ**

## 2.5 ตัวอย่างเขียนใหม่ — seg2 ของฉากศาลา

**เดิม (7 รอบ · เวลาหมดไปกับการเดิน):**

> *two workers walk in, cut open the boxes and lay out the panels and metal legs, then walk out;
> two workers lift it onto the platform, set it down along the LEFT side and walk out;
> two workers bolt a third bench together, carry it in, set it along the RIGHT side and walk out; …*

**ใหม่ (4 วรรค · ทั้งวรรคเป็นการทำ · เดินออกเฉพาะวรรคสุดท้าย):**

> *two workers walk in carrying the slatted panels and kneel on the platform, bolting the bench
> frame together and **tightening** each bolt in turn; a worker **slides** the finished bench along
> the LEFT side and **presses** it flush against the posts; two workers **tap** each table leg into
> its socket with the heel of one hand, then **smooth** the tabletop flat; a worker **peels** the blue
> protective film off the roof beams in one long strip, balls it up and walks out.
> No instant appearances. The final second is completely still and empty of people.*

## 2.6 `Subject:` — แก้ประโยคหลัง

**เดิม:**

> *No disembodied hands or arms — whenever an item is placed, **the whole person who placed it
> is visible in the frame**.*

**ใหม่:**

> *No disembodied hands or arms: **every arm in frame runs off the edge of the frame and stays
> attached to a body**, never ending in mid-air inside the frame.*

**ประโยคแรกเก็บไว้เหมือนเดิม** — สิ่งที่ห้ามคือแขนที่จบกลางอากาศ ไม่ใช่การที่ตัวอยู่นอกเฟรม
คลิปดัง 6/6 ใช้โคลสอัพที่แขนถูกขอบเฟรมตัด และไม่มีสักใบที่แขนตัดขาดกลางเฟรม
ประโยคหลังของเดิมกว้างเกินไปจนปิดโคลสอัพทุกแบบ และ**ขัดกับกฎ "อยากได้มือ ต้องเขียนคำว่ามือ"
ที่อยู่ในไฟล์เดียวกันอยู่แล้ว**

## 2.7 `Camera:` — กำกับเวลาโคลสอัพว่าทำอะไร (ทดลอง)

**ไม่ต้องมีโครงสร้างข้อมูลรองรับ** เขียนลงพรอมต์วิดีโอตรงๆ ว่า**วินาทีไหน ตัดไปดูมือทำอะไร**
ต่อท้ายบล็อก `Camera:` เดิม:

```
At about <วินาที> seconds the sequence cuts to a close shot of the hands <ทำอะไร>,
holding for about one second, then cuts back to the identical wide angle above,
framed exactly as before. <ทำซ้ำอีก 1-2 ครั้ง>
```

ตัวอย่างจริงของฉากศาลา seg2:

> *At about 2 seconds the sequence cuts to a close shot of the hands tightening a bolt on the
> bench frame, holding for about one second, then cuts back to the identical wide angle above,
> framed exactly as before. At about 5 seconds it cuts again to a close shot of the hands
> tapping a table leg into its socket, then cuts back to the same wide angle.*

**กติกา 3 ข้อ:**

1. **ระบุเวลาเป็นวินาที** ไม่ใช่ "ตอนกลางคลิป" — โมเดลต้องรู้ว่าวางตรงไหน
2. **ระบุว่ามือทำอะไร** ไม่ใช่แค่ "ตัดไปโคลสอัพ" — งานที่ทำคือสิ่งที่ทำให้ช็อตมีอะไรดู
3. **2–3 ครั้งต่อคลิป** และ**ห้ามอยู่ใน 1 วินาทีสุดท้าย** (รอยต่อ F2F)

**⚠️ ยังไม่มีสถิติรองรับ** — `seedance_2_5` ผ่าน 5/5 คลิปจากพรอมต์ที่เขียน
`fixed perspective throughout` ทุกใบ **ไม่เคยถูกสั่งให้คัตเลยสักครั้ง**
ต้องทดสอบตามลำดับในส่วนที่ 4 ห้ามใส่ทั้งสามคลิปพร้อมกัน

---

# ส่วนที่ 3 — ตัวตรวจ

## 3.1 `check-scene.mjs` ด่าน `clips` — เพิ่มการนับคัต

ตอนนี้ตรวจแค่ว่ากล้องไม่เลื่อน (เทียบเฟรมแรกกับเฟรมสุดท้าย)
พอเราเริ่ม**สั่งให้มีคัต** ต้องรู้ว่าโมเดลทำตามหรือไม่ ไม่งั้นจ่ายค่ายิงวิดีโอแล้วไม่รู้ผล

เพิ่มสามข้อ ใช้ scene-detection ตัวเดียวกับ `study-clip.mjs`:

| ข้อ | เกณฑ์ | ระดับ |
|---|---|---|
| จำนวนคัตในคลิป | สั่งไป N ครั้ง ได้ 0 = โมเดลไม่ทำตาม | **WARN** (ไม่ใช่คลิปเสีย แค่ไม่ได้ของที่ขอ) |
| คัตเกิน N+2 ครั้ง | ภาพเละ ตัดมั่ว | **FAIL** |
| **มีคัตใน 0.5 วิสุดท้าย** | เฟรมจบจะไม่ตรงภาพนิ่งเฟสถัดไป **รอยต่อ F2F ขาด** | **FAIL** |

ข้อสุดท้ายสำคัญที่สุด — เป็นทางเดียวที่การคัตจะทำลาย F2F ได้จริง

## 3.2 `study-clip.mjs` — ก๊อปตัวที่แก้บั๊กแล้วเข้า `starter/tools/`

รุ่นที่ปล่อยใน 1.9.0 มีบั๊กสองข้อที่แก้แล้วหลังปล่อย:
**ทิศการเคลื่อนกล้องกลับด้าน** (รายงานว่าถอย ทั้งที่เดินเข้า) และ **วัดสเกลจากแถบเดียว**
ต้อง sync เข้า `starter/tools/` ก่อนปล่อย 1.10.0

---

# ส่วนที่ 4 — ลำดับการทดสอบ ⭐ ห้ามข้าม

**วิดีโอคือด้านที่แพงที่สุด ยิงบนเว็บด้วยมือ เปลี่ยนหลายอย่างพร้อมกันแล้วพังจะไม่รู้ว่าข้อไหนผิด**

| รอบ | เปลี่ยนอะไร | ยิงกี่คลิป | ผ่านเมื่อ |
|---|---|---|---|
| **0** | **P4 หยุดซีด (1.1) — ยิงภาพอย่างเดียว ยังไม่แตะวิดีโอ** | **1 ภาพ** (~2 เครดิต) | `SATAVG ≥ 18` · เปิดดูแล้วสวยขึ้นจริง · หมุดยังอยู่ตำแหน่งเดิม |
| 1 | `Action:` ใหม่ (3–4 วรรค · เดินออกวรรคเดียว · กริยาจังหวะ · ของยาว ≤2 ชิ้น) + `Subject:` ใหม่ **ยังไม่สั่งคัต** | **1** (seg2 ฉากที่มีอยู่) | คนเต็มตัว ไม่มีแขนลอย · เฟรมจบตรง `phase3.png` · `check-scene --stage clips` เขียว |
| 2 | เพิ่มการกำกับเวลาโคลสอัพใน `Camera:` (2.7) | **1** (คลิปเดิม) | `study-clip` เห็นคัต 2–3 ครั้ง **ตรงวินาทีที่สั่งไป** · ไม่มีคัตใน 1 วิสุดท้าย · เฟรมจบยังตรง |
| 3 | ใช้ทั้งสามคลิป | 3 | ทุกด่านเขียว |

**รอบ 1 กับ 2 ใช้ฉากที่ทำเสร็จแล้ว** (`backyard-japanese-gazebo`) จะได้เทียบกับของเดิมตรงๆ
เก็บคลิปเดิมไว้ใน `clips/_rejected/` อย่าทับ

---

# ส่วนที่ 5 — ไฟล์ที่ต้องแก้

| ไฟล์ | แก้อะไร |
|---|---|
| `skills/idea/SKILL.md` | **กฎ P4 หยุดซีด 4 จุด (1.1)** · เลือกไอเดียที่สินค้ามีจังหวะกางออก (2.3) |
| `skills/phase2/SKILL.md` | P2 = งานค้างกลางจังหวะ ไม่ใช่วัสดุกองรอ (1.2) · **ไฟต้องติดแล้วตั้งแต่ P3** |
| `skills/phase3/SKILL.md` | **ไฟติดอยู่แล้วใน P3 · โคมไฟเป็นกลุ่ม `fixed` ไม่ใช่ `styling`** |
| `skills/storyboard/SKILL.md` | **ตารางแบ่ง 7 ขั้นลง 3 คลิป (2.3)** · กฎ `Action:` ใหม่ 8 ข้อ · เพดานของยาว 2 ชิ้น/คลิป · `Subject:` ประโยคหลัง · `Camera:` กำกับเวลาโคลสอัพ · ขั้นตรวจนับคัต |
| `starter/tools/check-scene.mjs` | นับคัต 3 ข้อ (3.1) · **`SATAVG` ของ `phase4.png` ≥ 18 (1.1)** |
| `starter/tools/study-clip.mjs` | sync ตัวที่แก้บั๊กแล้ว |
| `starter/CLAUDE.md` · `README.md` | ตารางกฎ + changelog |
| `starter/Timelapse_Blueprint_V1.md` | หัวข้อใหม่: สิ่งที่ถอดจากรีลเพจอื่น + ที่มาของกฎ `Action:` ใหม่ |

**ปล่อยเป็น 1.10.0** ด้วย `.\release.ps1 1.10.0 "<สรุป>"` เท่านั้น

---

---

# ส่วนที่ 6 — ข้อเสนอที่ยังไม่อยู่ในแผนนี้ (ยกไว้ให้ผู้ใช้ตัดสิน)

## 6.1 คลิปแบบ "จัดของอย่างเดียว" — สั้นกว่าครึ่ง และดังพอกัน

อ่าน grid ครบ 6 ใบแล้วพบว่า**มีสองแบบ ไม่ใช่แบบเดียว**:

| แบบ | คลิป | ความยาว | เริ่มจาก |
|---|---|---|---|
| สร้างแล้วค่อยจัด | 50m · 29m · 25m · 17m (4/6) | 29.5–30.2 วิ | พื้นที่ดิบ ดินเปล่า ปูนเปลือย |
| **จัดของอย่างเดียว** | 33m · 9m (2/6) | **15.1 · 17.6 วิ** | ห้องที่สร้างเสร็จแล้วแต่ยังโล่ง |

`33m` สั้นที่สุดในกลุ่ม (15 วิ) แต่ได้ยอดวิว**สูงเป็นอันดับสอง**

**โครง 4 เฟสของเราออกแบบมาสำหรับแบบแรกเท่านั้น** (P1 ว่างเปล่า → P2 ระหว่างทำ → P3 เสร็จ → P4 จัดของ)
ถ้าจะทำแบบที่สอง ต้องเป็นโครงใหม่ **2 เฟส 1 คลิป** — ยังไม่อยู่ในแผนนี้
แต่ราคาถูกกว่าครึ่ง (ยิงภาพ 2 ใบ ยิงวิดีโอ 1 คลิป) และสินค้าประเภทวางแล้วจบ
(ชิงช้า โคมไฟ ชั้นวาง) เหมาะกับแบบนี้มากกว่าแบบสร้าง

**ถามผู้ใช้:** อยากให้เพิ่มโครงนี้เป็นทางเลือกที่สองในปลั๊กอินไหม

---

## สิ่งที่แผนนี้ยังตอบไม่ได้

- **โมเดลวิดีโอจะยอมคัตในคลิปเดียวไหม** — ยังไม่เคยลอง รอบ 2 ของการทดสอบคือคำตอบ
- **คัตในคลิปดังมาจากโมเดลหรือจากการตัดต่อ** — จากไฟล์บอกไม่ได้
- **ถ้าโมเดลไม่ยอมคัต** ทางถอยคือทำที่ชั้น `assemble` (ยิงคลิปโคลสอัพแยกแล้วตัดแทรก)
  ซึ่งแพงกว่าเพราะต้องยิงเพิ่ม และต้องมีภาพนิ่งโคลสอัพเป็นหมุด — **ยังไม่อยู่ในแผนนี้**
