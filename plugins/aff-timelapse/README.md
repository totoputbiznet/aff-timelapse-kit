# aff-timelapse — ชุดเครื่องมือทำ Reels แนว timelapse ตกแต่งบ้าน

ปลั๊กอินของเพจ **Homey Vibes** ทำคลิป before/after การตกแต่งในบ้าน ระเบียงคอนโด และสวน
โดยมีสินค้าแอฟฟิลิเอตอยู่ในคลิป — เดินเรื่อง **4 เฟส 3 คลิป**
ว่างเปล่า → ระหว่างทำ → งานเสร็จแต่ยังไม่จัดของ → จัดของเสร็จ
ที่กล้องนิ่งอยู่มุมเดิมเป๊ะทั้งสี่เฟส

เอกสารหลักที่อธิบายเหตุผลเบื้องหลังทุกขั้น คือ `Timelapse_Blueprint_V1.md` ในโฟลเดอร์โปรเจค

## สกิลในชุดนี้

| คำสั่ง | ทำอะไร | เริ่มจาก |
|---|---|---|
| `/aff-timelapse:init` | **สร้างโฟลเดอร์งานใหม่** — ถามก่อนว่าจะเก็บไว้ที่ไหน แล้วก๊อป `starter/` ให้ | ไม่ต้องมีอะไรก่อน |
| `/aff-timelapse:scene` | **ครบจบในคำสั่งเดียว** — ร้อยทั้ง 7 ขั้น (มีจุดหยุดรอ 3 จุด) | ลิงก์ Shopee |
| `/aff-timelapse:product` | เก็บชื่อ ลิงก์ รูป และคำบรรยายดีไซน์ของสินค้า | ลิงก์ Shopee |
| `/aff-timelapse:idea` | ไอเดียแต่งบ้าน **4 แบบให้กดเลือก** → CAMERA LOCK CARD + พรอมต์ **Phase 4 (ภาพหมุด)** | โฟลเดอร์สินค้า |
| `/aff-timelapse:phase3` | พรอมต์ **แก้ภาพ** → งานเสร็จแต่ยังไม่จัดของ | โฟลเดอร์ฉาก |
| `/aff-timelapse:phase2` | พรอมต์ **แก้ภาพ** → ระหว่างทำ มีคนงาน | โฟลเดอร์ฉาก |
| `/aff-timelapse:phase1` | พรอมต์ **แก้ภาพ** → ว่างเปล่า ก่อนทำ | โฟลเดอร์ฉาก |
| `/aff-timelapse:storyboard` | สตอรีบอร์ด frame-to-frame **3 ช่วง ช่วงละ 8 วิ** + เสียง ASMR + **ขั้นตรวจคลิป** | โฟลเดอร์ฉาก |
| `/aff-timelapse:assemble` | รวมคลิปด้วย ffmpeg ส่งออก 9:16 | โฟลเดอร์ฉาก |
| `/aff-timelapse:study` | **ถอดความรู้จากรีลของเพจอื่น** — วัดโครงช็อต จังหวะคัต ขนาดภาพ เสียง → `timelapse_master_prompt.md` | ไฟล์ `.mp4` ใน `research/clips/` |

`/aff-timelapse:refsheet` ยังมีอยู่แต่**ไม่อยู่ในสายงานหลัก** เรียกเฉพาะตอนผู้ใช้ขอชีทเอง

`/aff-timelapse:study` เป็น**ขาเข้า** ไม่อยู่ในสายทำคลิป — เรียกเมื่อไหร่ก็ได้ที่เจอคลิปคนอื่นน่าศึกษา
ที่เหลือทั้งชุดเป็นขาออกคือตรวจงานที่เราผลิตเอง

## โมเดลกับความลึกที่แต่ละสกิลใช้

| สกิล | `model` | `effort` | เพราะ |
|---|---|---|---|
| `storyboard` | `opus` | `high` | พรอมต์วิดีโอคือของที่แพงที่สุด ยิงบนเว็บด้วยมือ พลาดแล้วเสียทั้งคลิป และมีกฎเยอะที่สุดในชุด |
| `phase1` `phase2` `phase3` | ตามเซสชัน | `high` | ต้องเขียนไม่เกิน ~1,200 ตัวอักษรโดยห้ามลืมของในกลุ่ม `product` — งานที่ต้องรอบคอบ |
| `idea` | ตามเซสชัน | `high` | คิดไอเดียให้ต่างกันจริงทั้ง 4 ข้อ |
| `assemble` · `init` | `sonnet` | ตามเซสชัน | งานกลไก — ต่อ ffmpeg · ก๊อปโฟลเดอร์ |
| `study` | ตามเซสชัน | `high` | ต้องแยกให้ออกว่า "ภาพใหญ่ขึ้น" มาจากคัตหรือจากการเคลื่อนกล้อง สรุปผิดข้อนี้ = เสียเงินยิงวิดีโอใหม่ |
| `product` | ตามเซสชัน | ตามเซสชัน | **จงใจไม่ลด** มันมีขั้นที่ต้องดูรูปด้วยตาแล้วนับชิ้นส่วน ซึ่งเป็นจุดที่สกิลเองบอกว่าพลาดแล้วแพงที่สุด |
| `scene` | ตามเซสชัน | ตามเซสชัน | ตั้งไปก็ไม่มีผล ดูข้อจำกัดข้างล่าง |

> ⚠️ **ค่าพวกนี้อยู่แค่จนจบเทิร์นปัจจุบัน** ไม่ได้บันทึกลง settings —
> พอผู้ใช้พิมพ์ข้อความถัดไป เซสชันกลับไปใช้ค่าเดิม
> สกิลที่**มีจุดหยุดรอผู้ใช้** (`scene` 3 จุด · `idea` ตอนกดเลือกไอเดีย) จึงคุมได้แค่ช่วงก่อนหยุดเท่านั้น
> ส่วนที่เหลือจะรันด้วยโมเดลของเซสชัน
>
> ถ้าค่าที่ระบุไม่มีในบัญชีผู้ใช้ Claude Code จะข้ามไปใช้โมเดลของเซสชันแทน **ไม่พัง**

## เกณฑ์ความตรงของสินค้า — ตัดสินแล้ว

**สินค้าขอแค่ "ใกล้เคียง" ไม่ต้องตรง 100%** ต้องตรงแค่ **ภาพลักษณ์โดยรวม** กับ **ฟังก์ชันการทำงาน**
จำนวนสัน จำนวนชั้น มิติเป๊ะๆ ไม่ต้องตรง — **สินค้าเป็นแค่ตัวอ้างอิง คลิปขายบรรยากาศและผลลัพธ์**

การไล่ให้ตรง 100% ทำให้เสียการยิงภาพหลายรอบโดยที่คลิปไม่คืบเลย
**ภาพผ่านเกณฑ์แล้วให้รับแล้วเดินต่อทันที**

## หลักที่ทั้งชุดยืนอยู่บน

**4 เฟส 3 คลิป · สร้าง Phase 4 ก่อน แล้วลบของออกถอยหลังไปเป็น Phase 3 · 2 · 1**

| เฟส | คืออะไร |
|---|---|
| P1 | ว่างเปล่า ก่อนทำ |
| P2 | ระหว่างทำ มีคนงาน เครื่องมือ วัสดุ |
| P3 | **งานเสร็จ แต่ยังไม่ได้จัดของ** |
| P4 | จัดของเสร็จ ← **ภาพหมุด สร้างก่อนเพื่อน** |

text-to-image สี่ครั้งได้กล้องคนละมุมแน่นอน ต่อให้พรอมต์เหมือนกันทุกตัวอักษร
ทางเดียวที่ล็อกมุมได้จริงคือ image-editing จาก `phase4.png`
**และทุกใบต้องแก้จากภาพหมุดโดยตรง ลึกแค่ทอดเดียว** ไม่ใช่ chain 1→2→3→4 ซึ่งสะสมความเพี้ยนสามทอด

**ทำไมต้องมี P3** — ถ้าให้คลิปเดียวทั้งทำงานให้เสร็จและจัดเฟอร์นิเจอร์ เฟอร์นิเจอร์จะ "โผล่มา"
แทนที่จะถูกยกเข้ามาวาง แยกแล้วแต่ละคลิปทำหน้าที่เดียว **และรอยต่อ P2→P3 กลายเป็นรอยต่อที่เนียนที่สุด**
เพราะทั้งสองฝั่งเป็นสภาพนิ่ง ไม่มีคนขวาง

สกิลทุกตัว**ไม่ได้ยิงภาพหรือวิดีโอเอง** — มันเขียนพรอมต์ให้ก๊อปไปวางบนเว็บ Higgsfield
เพราะโปร unlimited อยู่บนเว็บเท่านั้น (ยิงผ่าน CLI หรือ MCP คิดเครดิตเต็มราคา)

## สิ่งที่ต้องมีในเครื่องก่อนใช้

- **Chrome จริงของผู้ใช้** เชื่อมกับ `claude-in-chrome` — ใช้เปิดหน้า Shopee
- `node` — สคริปต์โหลดรูป `scripts/fetch-images.mjs`
- `ffmpeg` กับ `ffprobe` — ใช้ตอนรวมคลิปกับตรวจคลิป
- `codex` CLI ล็อกอิน ChatGPT — ใช้อ่านดีไซน์สินค้าจากรูป (ไม่มีก็ข้ามได้ แต่คุณภาพภาพจะตก)
- บัญชี **Higgsfield** — ยิงภาพกับวิดีโอบนเว็บ

**ชุดนี้เป็นก้อนเดียวจบตั้งแต่ 1.5.0** ไม่ต้องมีปลั๊กอิน `aff` อีกแล้ว

## เอาไปลงเครื่องใหม่ยังไง

**ทาง A — ก๊อปโฟลเดอร์** วางที่ `~/.claude/skills/aff-timelapse/` แล้วเปิดเซสชันใหม่
มันจะโหลดเองเป็น `aff-timelapse@skills-dir` ไม่ต้องติดตั้งอะไร

**ทาง B — ติดตั้งจาก marketplace** (อัปเดตทีหลังได้ด้วยคำสั่งเดียว)

```bash
claude plugin marketplace add <repo ที่แจก>
```

แล้ว `/plugin install aff-timelapse@homey-vibes`

ตรวจไฟล์ก่อนแจกทุกครั้ง:

```bash
claude plugin validate ~/.claude/skills/aff-timelapse
```

> **โครงโปรเจคมาพร้อมในตัวปลั๊กอินแล้ว** (`starter/`) — ติดตั้งเสร็จสั่ง `/aff-timelapse:init`
> แล้วมันจะถามว่าจะเก็บโฟลเดอร์งานไว้ที่ไหน แล้วก๊อป `CLAUDE.md` · `Timelapse_Blueprint_V1.md` ·
> `tools/` · `assets/fonts/` ให้ครบ
>
> **ตัวตรวจรันจากโฟลเดอร์งาน ไม่ใช่จากปลั๊กอิน** — `node tools/check-scene.mjs scenes/<slug>`
> และ `node tools/check-phase-prompt.mjs <scene-dir> prompts/phaseN.md` เป็นด่านบังคับของสายงาน

## กฎที่ต้องระวังตอนแก้ไฟล์ในชุดนี้

**ห้ามเขียนที่อยู่ไฟล์แบบตายตัว** และ**ห้ามอ้างไฟล์นอกโฟลเดอร์ปลั๊กอิน**
ให้ใช้ `${CLAUDE_SKILL_DIR}/../../references/<ไฟล์>` แทนเสมอ —
ตอนติดตั้งจาก marketplace ปลั๊กอินถูกก๊อปเข้า cache ของมันเอง path ที่ออกไปข้างนอกจะพังทันที

## ประวัติ

- **1.14.2** — **`phase2.png` ต้องคืบแค่หนึ่งในสามถึงครึ่ง ไม่ใช่เกือบครบ**
  - **ที่มา:** ฉากจริงที่ P2 โชว์งานเสร็จไป **8 ส่วนจาก 11 ส่วน** เหลืองานให้ `seg2` แค่ 2-3 ส่วน
    **ไม่พอถม 8 วินาที → โมเดลถมเวลาด้วยการติดตั้งซ้ำจุดที่ `seg1` ทำไปแล้ว**
    คนดูเห็นคนงานทำงานเดิมสองรอบ
  - ⚠️ **`check-scene.mjs` ให้ผ่านหมด ✅ 22 · ❌ 0** ในคลิปชุดนั้น — มันวัดความยาว เสียง เฟรมจบ
    จำนวนคัต ได้ครบ **แต่ไม่มีทางรู้ว่างานถูกทำสองรอบ** เจ้าของโปรเจคจับได้ด้วยตา
  - `phase2` — เพิ่มกฎ **ตั้งตัวเลขความคืบหน้าโดยถามว่า "เหลืออะไรให้ seg2 ทำ 8 วินาที"**
    ถ้าตอบได้แค่ "อีก 2 ชิ้น" = P2 คืบมากเกินไป ให้ลดลง · และ `seg1` ต้องกำกับว่าให้ทำแค่ชุดแรก
    **ไม่ใช่เร่งทำให้เกือบครบเพื่อวิ่งไปหาเฟรมจบ**
  - `storyboard` — เพิ่มขั้นตรวจด้วยตา **4.6 seg2 ทำงานซ้ำที่เดิมกับ seg1 ไหม** พร้อมลำดับการแก้:
    แก้พรอมต์ seg2 ก่อน (ยิงคลิปเดียว — ประกาศใน `Overall:` ว่างานเกือบเสร็จแล้ว · ระบุตำแหน่งที่เหลือ
    · เติมประโยคห้ามติดตั้งซ้ำ · เพิ่มงานเก็บงานมาถมเวลา) → ถ้ายังวนซ้ำจึงยิง `phase2.png` ใหม่
- **1.14.1** — **บังคับเซฟพรอมต์ P4 ที่ใช้ยิงจริงลง `prompts/phase4.md` ก่อนทำอย่างอื่น**
  ยิงผ่าน API ด้วย `--prompt-file` แล้วไฟล์ชั่วคราวหายไปกับ session — เกิดแล้วกับฉากหนึ่ง
  ยิงใบนั้นซ้ำให้เหมือนเดิมไม่ได้อีก · **และห้ามเอาไอเดียต้นทางมาใส่แทน**ถ้าพรอมต์ถูกเขียนใหม่ไปแล้ว
  เพราะจะได้ไฟล์ที่บรรยายคนละฉากกับภาพ ซึ่งแย่กว่าไม่มีไฟล์เลย
- **1.14.0** — **`tools/probe-band.mjs` — กวาดหา `static_band` ให้เอง + ฝังเป็นขั้นบังคับในสายงาน**
  ```bash
  node tools/probe-band.mjs scenes/<slug> --heatmap
  ```
  - กวาดครึ่งบนเป็นตาราง วัด SSIM **ทุกช่อง** ตามคู่ในโซ่ → ต่อช่องที่นิ่งเป็นกล่อง
    → คืน `static_band` ให้ก๊อปวาง + `frames/_band-probe.png` (เขียว = นิ่ง · แดง = ไม่นิ่ง)
  - **ลองหลายเส้นแบ่งแล้ววัดจริงทุกแบบก่อนเลือก** ไม่เชื่อเส้นเดียว
    (ฉบับแรกใช้ค่ากลางเป็นเส้นแบ่ง หลวมจนเสนอกล่องที่แย่กว่าที่คนเลือก — แก้แล้ว)
    · **ถ้าของเดิมดีกว่ามันบอกว่าอย่าเปลี่ยน** แทนที่จะเสนอของแย่กว่า
  - เอามาใช้กับสามฉากที่ทำเสร็จแล้ว **ชนะที่เลือกด้วยมือทั้งสามฉาก**
    `carport` 0.080 → **0.072** · `storeroom` 0.038 → **0.019** · `zen` 0.058 → **0.017**
  - **ฝังเป็นขั้นบังคับแล้ว** — `idea` ให้ร่าง `static_band` ตอนเขียน `phase4.json`
    (ตอนนั้นมี P4 ใบเดียว ยังวัดไม่ได้) · `scene` สั่งให้ **ปรับใหม่ทั้งหมดด้วย `probe-band`
    ทันทีที่ได้ภาพครบสี่ใบ** ไม่ต้องรอให้สั่ง แล้วยืนยันด้วยตาจากทั้งสองภาพก่อนรับ
  - **แก้กฎที่ตัวเองเพิ่งเขียนใน 1.13.1** — ลำดับความน่าเชื่อของพื้นผิวเป็นแค่**จุดตั้งต้น**
    ไม่ใช่คำตัดสิน · กล่องที่ชนะของ `carport` คาบเถาวัลย์อยู่บ้าง แต่ยังชนะทุกกล่องที่เลือกด้วยมือ
    ตามกฎ "เลี่ยงใบไม้" **ตัวตัดสินคือค่าที่วัดได้ของโซนนั้นจริงๆ**
  - `scene` — แก้จุดหยุดที่ 2 ที่ยังบอกให้ยิงทั้งสามใบจาก `phase4.png` ให้ตรงกับโซ่ `P4→P3→P1→P2`
- **1.13.1** — **บทเรียนใหม่: ของที่เป็น `fixed` ก็ยังเชื่อไม่ได้ทุกชนิด — ใบไม้กับเงาใบไม้ไม่นิ่ง**
  เจอตอนพยายามทำโซนหลายกล่องให้ฉากที่ส่วนบนเป็นเถาวัลย์ แล้วทุกแบบแย่ลง จึงวัดแยกทีละพื้นผิว:

  | พื้นผิวในโซน | ห่างกันมากสุด |
  |---|---|
  | **ผนังฉาบเรียบล้วน** | **0.099** ใช้ได้ |
  | ยอดไม้พื้นหลัง + ท้องฟ้า | 0.082 ใช้ได้ (ไกลและเบลอ) |
  | **เถาวัลย์ + ใบไม้ใกล้เลนส์** | **0.148** 🟡 |
  | **ผนังที่มีเงาใบไม้ทาบ** | **0.253** ⛔ แย่สุด |

  - เถาวัลย์กับต้นไม้อยู่ครบทั้งสี่เฟสจริง แต่ **โมเดลวาดใบขึ้นใหม่ทุกครั้ง** ใบไม่เคยตกที่เดิม
    **เงาที่มันทาบลงผนังจึงไม่เคยตรง** และตกหนักกว่าตัวใบไม้เองเพราะเงากินพื้นที่กว้างกว่า
  - **ลำดับความน่าเชื่อของพื้นผิว:** ผนัง/ฝ้า/รั้วทึบ/พื้นเรียบ → ท้องฟ้ากับฉากหลังไกล
    → (เลี่ยง) ใบไม้ใกล้ → (ห้าม) ผนังที่มีเงาใบไม้ทาบ
  - ⚠️ **หลายกล่องไม่ได้ดีกว่าเสมอ** — มันเป็นเครื่องมือ**เว้นของ** ไม่ใช่เครื่องมือ**ขยายโซน**
    ฉากที่ส่วนบนเป็นต้นไม้จะได้โซนแคบ **และนั่นถูกแล้ว**
- **1.13.0** — **เครื่องมือใหม่ `tools/contact-sheet.mjs` — แผ่นเทียบ 4 เฟสที่วาดกรอบโซน SSIM ทับให้เห็น**
  ```bash
  node tools/contact-sheet.mjs scenes/<slug>     # → frames/_contact-sheet.png
  ```
  - สี่เฟสเรียงตามเวลาเดินเรื่อง · **กรอบแดงทับโซนที่ด่าน SSIM วัดจริง** · ค่า SSIM พิมพ์กำกับทุกใบ
  - อ่าน `static_band` กับ `chain` จาก `phase4.json` **ใบเดียวกับที่ `check-scene.mjs` ใช้**
    จึงไม่มีทางเห็นคนละโซนกัน · ไม่มีฟอนต์ใน `assets/fonts` ก็ยังทำแผ่นได้ แค่ไม่มีตัวหนังสือ
  - ⚠️ เป็นแผ่นตรวจงาน **ไม่ใช่ของส่งออก** — `final/reel.mp4` ยังห้ามมีตัวหนังสือเหมือนเดิม
  - **ที่มา:** `check-scene.mjs` ตอบว่าผ่านไม่ผ่านเป็นตัวเลข แต่ตอบไม่ได้ว่าโซนที่วัดครอบอะไรอยู่
    ซึ่งเป็นตัวแปรที่ทำให้ตัวเลขเชื่อถือได้หรือไม่ได้
  - **`static_band` ใส่เป็น array หลายกล่องได้แล้ว** — วัดทีละกล่องแล้วเฉลี่ยถ่วงน้ำหนักด้วยพื้นที่
    ```json
    "static_band": [ {"x":0,"y":0,"w":0.48,"h":0.25}, {"x":0.78,"y":0,"w":0.22,"h":0.25} ]
    ```
    **กฎการแบ่งโซน: วัตถุที่ไม่ใช่ `fixed` แล้วมีบางส่วนโผล่เข้าโซนห้ามเปลี่ยน
    ให้นับเป็นของฝั่งเปลี่ยนได้ แล้วเว้นตัวมันออกจากโซนที่วัด** · ดู `from_phase` เป็นตัวชี้
    ถึงของชิ้นนั้นจะถูกวางไว้ในลิสต์ `fixed` ก็ตาม
    ⛔ ห้ามแก้ด้วยการ**ระบายทับ**ตัววัตถุ เพราะพิกเซลที่เหมือนกันเป๊ะได้ SSIM 1.0 แล้วเจือค่าจริงให้สูงเกินจริง
  - **บทเรียนใหม่ลง Blueprint — โซนไม่ต้องสะอาดสมบูรณ์ ให้ดู*ขนาด*ของสิ่งที่เปลี่ยน**
    วัดแถบสูงเท่ากันต่างกันแค่มีของที่เปลี่ยนอยู่ในกรอบไหม ได้ผลต่างกัน **0.005-0.008 = noise**
    · ของชิ้นเดียวเล็กๆ ปล่อยได้ · ที่ทำให้พังคือของที่**กระจายทั่วแถบ**
- **1.12.1** — **ซิงก์ `starter/Timelapse_Blueprint_V1.md` ให้ตรงกับฉบับที่ใช้งานจริง** (ตามหลังอยู่ ~190 บรรทัด)
  - เพิ่ม **ฉากพื้นหลังนิวทรัลต้องเลือกสีสินค้าโทนเข้มตั้งแต่ใบแรก** — `SATAVG` ตกได้ตั้งแต่ยังไม่ทันจัดของ
    ถ้าปูน/ผนังฉาบกินเกินครึ่งเฟรม (วัดได้ 11.2 → 18.7 หลังแก้ 4 อย่าง)
  - เพิ่ม **`no X` ผ่านตัวตรวจ แต่ไม่ได้แปลว่าโมเดลจะทำตาม** — ของที่โมเดลเชื่อว่า "ต้องมี"
    (สายไฟ หัวเตียง) สั่งห้ามเท่าไหร่ก็ไม่หาย **ต้องบอกว่าตรงนั้นมีอะไรอยู่แทน**
  - เพิ่ม **สั่งคัตไปโคลสอัพใน `Camera:` ได้ผลจริง** พร้อมสถิติรายวินาที และข้อยกเว้นที่รับได้ของคัตท้ายคลิป
  - เพิ่มหัวข้อ **นิยามของคำว่า "ผ่าน"** ฉบับเต็มใน OUTPUT 3.2 (เดิม 1.12.0 ใส่ไว้แค่ในเช็คลิสต์)
  - **แก้ที่ขัดกันเอง** — กฎทองข้อ 1 กับสรุป V2 ข้อ 1 ยังเขียนว่า "แก้จากหมุดโดยตรงทุกใบ"
    ทั้งที่โซ่จริงคือ `P4 → P3 → P1 → P2` · และบันทึกเรื่องสองอ้างอิงยังพาดหัวว่าเป็นวิธีที่แนะนำ
    ทั้งที่หัวข้อถัดไปพิสูจน์แล้วว่าตกรอบ → เขียนใหม่เป็นบันทึก "ลองแล้วไม่ผ่าน อย่าลองซ้ำ"
- **1.12.0** — **นิยามของคำว่า "ผ่าน" กลายเป็นด่านที่วัดได้ + แก้โซ่ภาพตั้งต้นให้ตรงกับที่ใช้จริง**
  - `check-scene.mjs` — ด่าน `frames` เพิ่ม **`องค์ประกอบโซนที่ห้ามเปลี่ยน`** วัด **SSIM**
    ของโซนที่ไม่มีงานเกิดขึ้นเลย เทียบกับ**ใบต้นทางในโซ่** (`P1→P3` `P2→P3` `P3→P4`)
    · ห่างจากใบดีสุดของฉาก **>0.10 = WARN · >0.15 = FAIL**
    · ทับค่าได้ที่ `phase4.json` คีย์ **`static_band`** (ปริยายบน 40%) และ **`chain`**
  - **ทำไมต้องมี** — ใบ P2 ที่ยิงด้วย**สองอ้างอิง**ได้ SSIM **0.312 แย่สุด** ในสี่ใบที่เทียบ
    **แต่ผ่านหมุดครบทั้งสามเส้น** เพราะคอลัมน์ที่เลือกบังเอิญตกบนขอบที่ตรงกัน
    **หมุดเป็นสายสะดุด ไม่ใช่คำนิยามของคำว่าผ่าน**
  - **ตัดสินด้วยอันดับในฉากเดียวกัน ไม่ใช่ค่าสัมบูรณ์** — ทุกใบเป็น generative edit
    ที่เรนเดอร์ใหม่ทั้งภาพ ฉากที่รับแล้วอยู่ได้ตั้งแต่ **0.568 ถึง 0.901** ตามความละเอียดของโซน
  - `phase1` · `phase2` — **ภาพตั้งต้นเปลี่ยนจาก `phase4.png` เป็น `phase3.png`**
    โซ่ที่ใช้จริงคือ **`P4 → P3 → P1 → P2`** ทุกใบแก้จากภาพเดียว **ห้ามใส่สองอ้างอิง**
    (จาก P4 ต้องลบของแต่ง+สินค้าพร้อมกัน โมเดลวาดฉากใหม่ หมุดคลาด 13-20%
    · จาก P1 ไม่มีสินค้าให้ก๊อป ได้พื้นคนละวัสดุ YAVG 86.2/SAT 15.3 เทียบ P3 ที่ 97.5/25.0)
  - `phase3` — ย้ำว่าเป็น**ใบเดียวที่แก้จากภาพหมุดโดยตรง** และเป็นต้นทางของอีกสองใบ
  - `idea` — คำสั่งถัดไปแก้จาก `phase1` เป็น **`phase3`** ให้ตรงกับโซ่
- **1.11.0** — สตอรีบอร์ดต้องมี**ตารางอ่านเป็นไทยทุกช่วง** (ไทยกำกับ อังกฤษกำกับยิง) ·
  คำสั่งคัตมีสถิติรองรับแล้วจึงใส่ได้ทั้งสามใบ · ตัดกฎของบังหน้าเฟรมที่ทำให้โมเดลย้ายกล้อง ·
  `SATAVG` วัดเฉพาะกลางเฟรม 60%×50%
- **1.10.0** — **ปรับตามสิ่งที่ถอดจากรีลจริง 6 ตัวของเพจอื่น (9–50 ล้านวิว)**
  ทุกข้อมีตัวเลขกำกับ ดู `timelapse_master_prompt.md` ในโปรเจค
  - `storyboard` — **ลำดับ A/B/C ลง 3 คลิป** · `seg1` เตรียมพื้นที่+ลงพื้น(เริ่ม) ·
    `seg2` ลงพื้น(จบ)+งานยึดติด+ไฟผนังติด · `seg3` ของหนัก→ของเล็ก→ชิ้นเอกที่กางออก
    **ขั้นลงพื้นคร่อมสองคลิปโดยตั้งใจ** เพราะกินเวลา 8.5–11 วิ เกินเพดาน ~10 วิของโมเดล
  - `storyboard` — `Action:` กฎเพิ่มจาก 4 เป็น **8 ข้อ** · **เดินออกเฉพาะวรรคสุดท้าย**
    (เดิม 7 รอบ `walk in…walk out` ใน 8 วิ = เวลาหมดไปกับการเดิน) · **3–4 วรรค** ·
    ทุกวรรคต้องมีกริยาจังหวะ · **กริยาตรงกับน้ำหนักของ** (หนัก = `drags` ไม่ใช่ `carries`) ·
    **ของที่กินเวลา ≥2 วิ ไม่เกิน 2 ชิ้น/คลิป**
  - `storyboard` — `Subject:` **แขนถูกขอบเฟรมตัด ≠ แขนตัดขาดกลางเฟรม**
    ประโยคเดิม `the whole person is visible in the frame` กว้างเกินไปจนปิดโคลสอัพทุกแบบ
    ทั้งที่รีลจริง 6/6 ใช้โคลสอัพมือ และไม่มีสักใบที่แขนจบกลางอากาศ
  - `storyboard` — `Camera:` **กำกับเวลาโคลสอัพเป็นวินาที + บอกว่ามือทำอะไร** 2–3 ครั้ง/คลิป
    **ห้ามมีคัตใน 1 วิสุดท้าย** (ทางเดียวที่การคัตจะทำลายรอยต่อ F2F ได้) · **ยังไม่มีสถิติรองรับ ทดสอบทีละคลิป**
  - `idea` — **P4 ต้องไม่ซีด 4 ข้อ** วัดแล้วความอิ่มสีภาพจบเขาค่ากลาง 22.5 ของเรา 12
    (ไฟดวงจริงติดในเฟรม ≥2 ดวง · ของแต่ง ~10–12 ชิ้นเป็นชั้น · ผ้ามีลายมีสี ≥1 · ของบังหน้าเฟรม)
    · ให้น้ำหนักไอเดียที่สินค้ามีจังหวะกางออก
  - `phase3` — **ไฟที่ยึดติดอาคารต้องติดสว่างแล้วใน P3** เป็นกลุ่ม `fixed` ไม่ใช่ `styling`
    (รีลจริงไฟผนังติดตั้งแต่ 8.5–10 วิ แล้วของที่จัดทีหลังได้แสงอุ่นนั้นทั้งหมด)
  - `phase2` — P2 = **งานที่ค้างอยู่กลางจังหวะ** ไม่ใช่กองของรอ · เครื่องมือวางตรงรอยต่อที่หยุดทำ
  - `check-scene.mjs` — ด่าน `frames` เพิ่ม **`SATAVG ≥ 18`** · ด่าน `clips` เพิ่ม **นับคัต**
    (0 คัต = WARN · เกิน 5 = FAIL · **คัตใน 1 วิสุดท้าย = FAIL**)
  - `study-clip.mjs` — แก้บั๊ก **ทิศการเคลื่อนกล้องกลับด้าน** (รายงานว่าถอย ทั้งที่เดินเข้า) ·
    วัดสเกล 3 แถบแล้วเอาค่ากลางแทนแถบเดียว · เพิ่ม **ตารางเฟรมทุก 0.5 วิพร้อมเวลากำกับ**
- **1.9.0** — เพิ่มสกิล **`study`** — ถอดโครงช็อตจากรีลของเพจอื่น (ขาเข้าตัวแรกของชุดนี้)
  พร้อม `tools/study-clip.mjs` วัดจำนวนช็อต · ความยาวช็อต · การเคลื่อนกล้อง · เสียง
- **1.8.0** — **ตามกฎของ `CLAUDE.md` ให้ทัน + เดินงานต่อเองโดยไม่ต้องขออนุญาตทุกขั้น**
  - ทุกสกิลได้หัวข้อ **"จังหวะการทำงาน — รายงานแล้วไปต่อเอง"** — จบขั้นไหนรายงานผลที่วัดได้
    1-3 บรรทัดแล้วทำต่อทันที **หยุดเฉพาะ 3 กรณี** (ผู้ใช้ต้องตัดสินใจ · ต้องไปยิงเองบนเว็บ ·
    ติดปัญหาที่แก้เองไม่ได้) และห้ามถามว่า "จะให้ทำต่อไหม"
  - **พรอมต์ภาพเป็น JSON ทุกใบแล้ว** — เดิม `idea`/`refsheet` ยังใช้บล็อก `[CAMERA]` แบบเก่า
    และ `phase1-3` ใช้ `Scene:/Subject:/Camera:` ซึ่งขัดกับกฎในโปรเจคที่บังคับ JSON มานานแล้ว
    คีย์ตามสเปก: `reference camera scene subject product palette lighting style` · ใบ P4 เพิ่ม
    `critical` กับ `negative`
  - **ค่าในทุกคีย์ยังเป็นภาษาอังกฤษ** ส่วนหัวข้อ คำอธิบาย ตาราง และช่องที่ให้เติมเป็นไทย —
    สถิติที่วัดมา (JSON กับ `nano_banana_2` ผ่าน 8/8 ใบ) มาจากพรอมต์อังกฤษทั้งหมด
  - ⛔ **พรอมต์วิดีโอยังห้ามแปลงเป็น JSON** เขียนกำกับไว้ใน `storyboard` แล้ว
    8 บล็อกเป็น named fields อยู่แล้ว และยังไม่เคยทดสอบ JSON กับโมเดลวิดีโอเลย
  - เพิ่ม **ขั้นทำ `frames/phase4.json`** ลงใน `idea` (บัญชีของทุกอย่างในภาพหมุด พร้อมสเปกคีย์
    `pins fixed product styling lighting uncertain`) — เดิมไม่มีสกิลไหนพูดถึงไฟล์นี้เลย
    ทั้งที่ตัวตรวจทั้งสองตัวต้องใช้
  - เพิ่ม **ด่านรัน `check-phase-prompt.mjs` ก่อนส่งพรอมต์ให้ยิง** ใน `phase1-3` และ `scene`
    ของในกลุ่ม `product` ที่พรอมต์ไม่เอ่ยถึง = ERROR ต้องแก้ให้หมดก่อน
- **1.7.0** — **กำหนดโมเดลกับความลึกให้แต่ละสกิล** (ดูตารางข้างบน)
  - `storyboard` → `model: opus` + `effort: high` · `phase1-3` กับ `idea` → `effort: high`
  - `assemble` กับ `init` → `model: sonnet` งานกลไกไม่ต้องใช้ของแพง
  - **`product` จงใจไม่ลด** ถึงจะดูเป็นงานกลไก แต่มันมีขั้นที่ต้องดูรูปด้วยตาแล้วนับชิ้นส่วน
    ซึ่งสกิลเองระบุว่าเป็น "กฎที่แพงที่สุด" — เคยนับสันผิดจนเสียการยิงภาพ 3 รอบมาแล้ว
  - ใช้อาลิอัส `opus`/`sonnet` ไม่ใช่ชื่อรุ่นเต็ม เพราะอาลิอัสตามรุ่นล่าสุดของแต่ละ provider ให้เอง
- **1.6.0** — **ให้ผู้ใช้กดเลือกแทนการพิมพ์ตอบ** ในจุดที่ต้องตัดสินใจ
  - `idea` — เหลือ **4 ไอเดีย** แล้วถามผ่านกล่องเลือก `AskUserQuestion`
    รายละเอียดแต่ละไอเดียไปอยู่ในช่อง `preview` (พื้นที่ · สินค้าอยู่ตรงไหน · ทำไมจะจี๊ด)
    **4 คือเพดานของเครื่องมือ** ใส่ได้มากสุด 4 ตัวเลือก และระบบเติมช่อง `Other` ให้เองเสมอ
    ซึ่งกลายเป็นช่องที่ผู้ใช้พิมพ์โจทย์ของตัวเองได้ แทนกฎเดิมที่ต้องดักว่า "ผู้ใช้ตอบอย่างอื่น"
  - เพิ่มสกิล **`init`** — สร้างโฟลเดอร์งานใหม่จาก `starter/` โดย**ถามก่อนว่าจะเก็บไว้ที่ไหน**
    และไม่เขียนทับของเดิมในทุกกรณี
  - ย้าย `starter/` (คู่มือ · ตัวตรวจ · ฟอนต์) **เข้ามาไว้ในตัวปลั๊กอิน**
    เพราะตอนติดตั้งจาก marketplace จะก๊อปเฉพาะโฟลเดอร์ปลั๊กอิน ของที่อยู่นอกนั้นไปไม่ถึงผู้ใช้
- **1.5.0** — **ตัดสายพึ่งปลั๊กอิน `aff` ทิ้ง ชุดนี้เป็นก้อนเดียวจบแล้ว** แจกต่อได้โดยไม่ต้องแถมอะไร
  - ย้ายของที่เคยยืมมาไว้ในตัวเอง: `references/shopee-collect.md` (เก็บสินค้า 5 ขั้น) ·
    `references/refsheet-guide.md` + `references/ref-sheet-template.md` · `scripts/fetch-images.mjs`
  - path ในเอกสารที่ย้ายมาแก้เป็น `products/<slug>/` แล้วทุกจุด ไม่ต้องแปลงเองตอนอ่านอีก
  - **เหตุผล** — เอกสาร Claude Code ระบุว่าตอนติดตั้ง ปลั๊กอินถูกก๊อปเข้า cache
    การอ้าง `../../../aff/` จึงพังทันทีที่เพื่อนติดตั้งจาก marketplace
- **1.4.1** — `assemble` — **`final/reel.mp4` ต้องไม่มีตัวหนังสือบนจอ** ส่งไฟล์ภาพกับเสียงล้วน
  ผู้ใช้จัดการชั้นตัวหนังสือเองตอนโพสต์ · ห้ามใส่ให้เอง ห้ามเสนอ
  (วิธีใส่ด้วย ffmpeg + ฟอนต์ไทยใน `assets/fonts/` ยังเก็บไว้ในสกิล เผื่อผู้ใช้สั่งมาเอง)
- **1.4.0** — **4 เฟส 3 คลิป + กฎ active voice + ขั้นตรวจคลิป** (ปรับหลังศึกษาเทมเพลต sjinn.ai)
  - เพิ่มสกิล **`phase3`** งานเสร็จแต่ยังไม่จัดของ · ภาพหมุดย้ายไปเป็น **P4**
  - `storyboard` — 3 ช่วง ช่วงละ 8 วิ · โครงพรอมต์ 8 บล็อก
    `Overall/Camera/Subject/Action/Lighting/Sound Effects/Dialogue:No/Subtitles:No`
  - `storyboard` — ⛔ **`Action:` ต้องเป็น active voice ขึ้นต้นทุกวรรคด้วยคนทำ**
    passive voice ทำให้ได้**ท่อนแขนลอย ไม่มีคนเดินเข้ามาสักครั้ง** เพราะมันลบคนทำออกจากประโยค
  - `storyboard` — เพิ่ม **ขั้นตรวจคลิป** ด้วย `ffprobe` · `volumedetect` · เฟรมรอยต่อ ·
    difference blend หากล้องขยับ · contact sheet ดูว่าคนเต็มตัวไหม **อย่าตรวจด้วยตาอย่างเดียว**
  - `storyboard` — **ห้ามยิงยาวแล้วตัดท้าย** เฟรมสุดท้ายคือภาพเฟสถัดไป ตัดแล้วโซ่ขาด
  - `assemble` — เปลี่ยนเป็น **ffmpeg** ตามที่ใช้จริง (hyperframes เหลือไว้ใส่ตัวหนังสือไทย)
    พร้อมคำสั่งเต็มที่ทำงานได้จริง
- **1.3.0** — **โครงพรอมต์แก้ภาพใหม่ทั้งหมด** หลังเจอ Generation failed สองใบรวด
  - พรอมต์แก้ภาพที่ยาว ~2,000 ตัวอักษรและมีบล็อกคำสั่งห้ามเยอะ **ทำให้ failed ไปเลย
    ไม่ใช่แค่ผลไม่ดี** · ตัดเหลือครึ่งเดียวเขียนเป็นประโยคบอกเล่า ผ่านทันทีรอบเดียว
  - `phase1` + `phase2` — เปลี่ยนเป็นโครง 5 บรรทัด
    `Reference Image 1.` / `Scene:` / `Subject:` / `Camera:` / `Lighting:` / `Style:`
    หมุดอ้างอิงย้ายไปรวมเป็นประโยคเดียวในบรรทัด `Camera:` ไม่ใส่พิกัด %
  - บันทึกว่า **failed generation ไม่โผล่ใน `show_generations`** (API คืนเฉพาะที่สำเร็จ)
    เจอ failed ให้แก้พรอมต์เลย อย่าเสียเวลาหา error message
- **1.2.0** — **ปรับสโคปตามที่เจ้าของโปรเจคตัดสิน: สินค้าขอแค่ใกล้เคียง ไม่ต้องตรง 100%**
  - `idea` — เพิ่มเกณฑ์รับภาพ (ภาพลักษณ์รวม + ฟังก์ชัน) และห้ามใช้ตัวเลขเป็นเกณฑ์ตัดสินรับ/ไม่รับ
  - `scene` — **ถอด `refsheet` ออกจากสายงานหลัก** เหลือ 3 จุดหยุดเหมือนเดิม
  - `refsheet` — ยังอยู่แต่ปรับ description เป็นเรียกเฉพาะตอนผู้ใช้ขอเอง ห้ามเสนอเอง
  - เหตุผล: การไล่ให้สินค้าตรง 100% ทำให้เสียการยิงภาพไปหลายรอบโดยที่คลิปไม่คืบเลย
- **1.1.1** — แก้จากรอบทำชีทจริง (root cause ของภาพที่พังมา 3 รอบ)
  - `product` — **ห้ามนับชิ้นส่วนจากรูปแบนใบเดียว** ต้องมีรูปหน้าตัด/ด้านข้าง หรือแบบเทคนิคยืนยันก่อน
    ไม่มีให้เขียนว่าไม่รู้ ห้ามเดา · สองแหล่งขัดกันให้เชื่อแบบเทคนิค
  - `product` + `refsheet` — **แนบ reference หลายใบจากหลายมุม** ใบเดียวไม่พอ
    แต่ห้ามแนบรูปแบบเทคนิคที่มีตัวเลข และห้ามแนบรูปใช้งานจากหน้าร้านที่ขัดกับสินค้าจริง
  - `idea` — **กล่องโค้ดต้องเป็นพรอมต์เต็มใบเสมอ** ห้ามโชว์เฉพาะบล็อกที่แก้
    (เกิดขึ้นจริง: ผู้ใช้ก๊อปเฉพาะ `[PRODUCT]` ไปวาง ได้ทางเดินออฟฟิศแทนระเบียง)
  - `refsheet` — เพิ่มข้อตรวจ นับจำนวนชิ้นส่วนทุกมุมแล้วเทียบกันเอง
- **1.1.0** — เพิ่มสกิล `refsheet` — ชีทรูปสินค้า 4 มุม เป็น reference หลักของ Phase 3
  - เป็นตัวบางๆ ที่ต่อยอด `/aff:refsheet` ไม่ได้เขียนวิธีใหม่ — เปลี่ยนแค่ path
    (`products/<slug>/`) กับตัวตั้งต้น (`design.md`)
  - ของใหม่: **เลือกมุมให้พิสูจน์สิ่งที่โมเดลชอบเข้าใจผิด** พร้อมตารางคู่สินค้าที่สับสนบ่อย
    (คำห้ามใน NEGATIVE กันไม่อยู่ แต่ภาพหน้าตัดกันอยู่)
  - แทรกเป็นขั้นที่ 1.5 ของ `scene` และ `idea` เตือนถ้ายังไม่มีชีท
- **1.0.2** — แก้จากรอบยิงภาพ Phase 3 จริง
  - ทุกสกิล — **ดึงผลจาก Higgsfield ด้วย MCP `show_generations` แทนการให้ผู้ใช้เซฟไฟล์เอง**
    การอ่านไม่กินเครดิต มีแต่การยิงที่กิน · ใช้ `results.rawUrl` ไม่ใช่ `minUrl`
    · ยืนยัน `params.prompt` ให้ตรงก่อนโหลด (ผู้ใช้อาจยิงงานอื่นคั่น)
  - `idea` — **`[NEGATIVE]` กันของที่ตีความผิดไม่อยู่ ต้องเขียนเชิงบวกใน `[PRODUCT]` ว่ามันคืออะไร**
    พร้อมวิธีหาว่าโมเดลสับสนสินค้าเรากับอะไร
  - `idea` — CAMERA LOCK CARD **บรรยายภาพที่รับแล้ว** ต้องกลับมาวัดตัวเลขจริงเขียนทับ
  - `idea` — ภาพที่ไม่ผ่านให้ย้ายไป `frames/_rejected/` พร้อมจดอาการ ไม่ใช่ลบทิ้ง
- **1.0.1** — แก้จากการเดินท่อจริงครั้งแรก (ระแนงไม้เทียม WPC → ระเบียงคอนโด)
  - `product` — เพิ่มขั้นครอปโซนสะอาดออกจากรูปที่มีตัวหนังสือ ก่อนจะสรุปว่า `seeds/` ว่าง
    (ร้านไทยแปะตัวหนังสือทับทุกรูปเป็นเรื่องปกติ ไม่ใช่กรณียกเว้น)
  - `product` — เตือนเรื่องเลื่อนหน้าเร็วเกินจนโดน `/verify/captcha` และจัดลำดับเก็บของให้เสี่ยงทีหลัง
  - `product` — ให้แนบแบบเทคนิคเข้า codex ด้วย และเชื่อตัวเลขจากแบบก่อนการนับจากรูปถ่าย
  - `idea` — ห้ามเลือกหมุดอ้างอิงบนพื้นผิวที่สินค้าไปติดทับ (ความหนาสินค้าดันแนวรอยต่อ)
- **1.0.0** — ชุดแรก 7 สกิล ตามเอกสาร `Timelapse_Blueprint_V1.md`
